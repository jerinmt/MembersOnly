<!DOCTYPE html>
<html>
  <head>
    <title>Home</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <h1>The Wall</h1>
    <% if (locals.user) { %>
      <h2>Welcome <%= locals.user.fname %></h2>
    <% } %>
      <form action="/" method="POST" class="newMessage
    <div class="board">
        <ul>
            <% for( let i = 0; i < messages.length; i++) { %>
            <%- include('messages', {message: messages[i]}); %>
            <% } %>
        </ul>
    </div>
  </body>
</html>